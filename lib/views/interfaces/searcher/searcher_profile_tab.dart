import 'package:flutter/material.dart';
import '../profile_interface.dart';

class SearcherProfileTab extends StatelessWidget {
  const SearcherProfileTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ProfileInterface();
  }
}
